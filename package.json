{
  "name": "the_photo_app",
  "version": "1.0.0",
  "description": "The Photo App - Marketplace",
  "scripts": {
    "backend": "cd backend && call venv/Scripts/activate.bat && uvicorn app.main:app --reload",
    "frontend": "cd frontend && npx expo start --clear"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prisma": "^7.8.0"
  }
}
